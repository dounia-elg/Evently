# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=evently_user
DB_PASSWORD=evently_password
DB_NAME=evently_db

# Security
JWT_SECRET=super_secret_key_change_me_in_production

# Frontend Configuration
# Use 'http://localhost:3000/api' for local development
# Use 'http://backend:3000/api' if strictly running within Docker networks (requires SSR adjustments)
NEXT_PUBLIC_API_URL=http://localhost:3000/api
